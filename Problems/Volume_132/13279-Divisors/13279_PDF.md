以下是 **UVA 13279 – Divisor** 的題目整理與翻譯，依照你的設定：**英文轉成 markdown，英文原文完整保留，並附上中文翻譯**。

---

# 13279 - Divisor

The number of divisor function or $d(n)$ is a very interesting function in number theory. It denotes the number of positive divisors of a particular number. For example $d(24) = 8$ as 24 has eight divisors 1, 2, 3, 4, 6, 8, 12 and 24. In mathematics factorial of a positive integer number $n$ is written as $n!$ and is defined as below:

$$  
n! = 1 \times 2 \times 3 \times \cdots \times n = \prod_{i=1}^{n} i  
$$

Another interesting function $AF(n)$ (Again factorial in short) is defined as:

$$  
AF(n) = 1! \times 2! \times 3! \times \cdots \times n! = \prod_{i=1}^{n} i!  
$$

Given $n$, your job is to find the value of $d(AF(n))$.

---

### 中文說明

因數個數函數（number of divisor function）$d(n)$ 是數論中一個非常有趣的函數，它表示一個正整數的**正因數個數**。  
例如 $d(24) = 8$，因為 24 有八個正因數：1, 2, 3, 4, 6, 8, 12, 24。

在數學中，正整數 $n$ 的階乘（factorial）表示為 $n!$，定義如下：

$$  
n! = 1 \times 2 \times 3 \times \cdots \times n = \prod_{i=1}^{n} i  
$$

另一個有趣的函數 $AF(n)$（Again factorial 的縮寫）定義為：

$$  
AF(n) = 1! \times 2! \times 3! \times \cdots \times n! = \prod_{i=1}^{n} i!  
$$

給定 $n$，你的任務是求出 $d(AF(n))$ 的值。

---

## Input

The input file contains at most 101 lines of inputs. Each line contains an integer $n$ $(0 < n < 5000001)$.  
Input is terminated by a line containing a single zero. This value should not be processed.

---

## Output

For each line of input produce one line of output.  
This line contains the modulo $100000007$ $(10^8 + 7)$ of $d(AF(n))$.

---

### 中文說明

對於每筆測資，輸出一行結果。  
該行為 $d(AF(n)) \bmod 100000007 ,(10^8 + 7)$ 的值。

---

如果你接下來需要的是：

- 🔍 **數學推導（如何算 $d(AF(n))$）**
    
- ⚙️ **時間複雜度分析**
    
- 💻 **C / C++ 解法（適合 UVA）**
    

直接跟我說你要哪一個，我接著幫你補齊 👍

### 中文說明

輸入檔最多包含 **101 行**。  
每一行包含一個整數 $n$，其中 $0 < n < 5000001$。  
當輸入為 **單獨一個 0** 時表示結束，此筆資料不需處理。

---

