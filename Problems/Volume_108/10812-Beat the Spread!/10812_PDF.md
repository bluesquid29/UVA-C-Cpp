# 10812 - Beat the Spread!

Superbowl Sunday is nearly here. In order to pass the time waiting for the half-time commercials and wardrobe malfunctions, the local hackers have organized a betting pool on the game. Members place their bets on the sum of the two final scores, or on the absolute difference between the two scores. 

Given the winning numbers for each type of bet, can you deduce the final scores?

---

## Input

The first line of input contains $n$, the number of test cases. $n$ lines follow, each representing a test case. Each test case gives $s$ and $d$, non-negative integers representing the sum and (absolute) difference between the two final scores.

## Output

For each test case, output a line giving the two final scores, largest first. If there are no such scores, output a line containing "impossible". Recall that football scores are always non-negative integers.

---

## Sample Input

```
2
40 20
20 40
```

## Sample Output

```
30 10
impossible
```

---

# 中文翻譯

超級盃星期日即將到來。為了打發等待中場廣告和服裝凸槌（wardrobe malfunctions）的時間，當地的駭客們組織了一個比賽賭盤。成員們針對兩支球隊最終比數的「和」或「差的絕對值」進行下注。

給定這兩類投注的獲勝數字，你能推算出最終的比分嗎？

---

## Input

輸入的第一行包含一個整數 $n$，代表測資的筆數。接下來有 $n$ 行，每行代表一筆測資。每筆測資包含兩個非負整數 $s$ 與 $d$，分別代表兩支球隊最終比數的和與差（絕對值）。

## Output

對於每筆測資，輸出一行包含兩個最終比分，較大的數字在前。如果不存在這樣的比分，則輸出一行 "impossible"。請記住，美式足球的比分始終為非負整數。
