以下依你的 **UVA 翻譯規則**來處理：

- 英文內容 **完全照原題**（轉成 Markdown）
    
- **中英對照**
    
- 使用 Markdown 標題
    
- 數學符號用 LaTeX
    
- 用你指定的偏好翻譯詞彙
    

---

# 10908 - Largest Square

## Problem Description

**English**

Given a rectangular grid of characters you have to find out the length of a side of the largest square such that all the characters of the square are same and the center \[intersecting point of the two diagonals\] of the square is at location $(r, c)$.  

The height and width of the grid is $M$ and $N$ respectively.  Upper left corner and lower right corner of the grid will be denoted by $(0, 0)$ and $(M - 1, N - 1)$ respectively.

Consider the grid of characters given below. Given the location $(1, 2)$ the length of a side of the largest square is 3.

```
abbbaaaaa
abbbaaaaa
abbbaaaaa
aaaaaaaaa
aaaaaaaaa
aaccaaaaa
aaccaaaaa
```

---

**中文**

給定一個由字元組成的矩形網格，你必須找出**邊長最大的正方形**，使得該正方形內的所有字元都相同，且正方形的中心（兩條對角線的交點）位於 $(r, c)$。

網格的高度與寬度分別為 $M$ 與 $N$。左上角與右下角分別表示為 $(0, 0)$ 與 $(M - 1, N - 1)$。

考慮上方所示的字元網格，若給定位置 $(1, 2)$，則符合條件的最大正方形邊長為 **3**。

---

## Input

**English**

The input starts with a line containing a single integer (T) ((< 21)).  
This is followed by (T) test cases.

The first line of each of them will contain three integers (M, N) and (Q) ((< 21)) separated by a space where (M, N) denotes the dimension of the grid.  
Next follows (M) lines each containing (N) characters.  
Finally, there will be (Q) lines each containing two integers (r) and (c).

The value of (M) and (N) will be at most 100.

---

**中文**

輸入的第一行包含一個整數 (T)（測資的筆數，(T < 21)）。  
接下來會有 (T) 組測資。

每筆測資的第一行包含三個整數 (M, N, Q)（皆小於 21），其中 (M, N) 表示網格的維度。  
接下來有 (M) 行，每行包含 (N) 個字元。  
最後有 (Q) 行，每行包含兩個整數 (r) 與 (c)。

(M) 與 (N) 的最大值為 100。

---

## Output

**English**

For each test case in the input produce (Q + 1) lines of output.  
In the first line print the value of (M, N) and (Q) in that order separated by single space.  
In the next (Q) lines, output the length of a side of the largest square in the corresponding grid for each ((r, c)) pair in the input.

---

**中文**

對於每筆測資，請輸出 **(Q + 1)** 行結果。  
第一行依序輸出 (M, N, Q)，並以單一空白分隔。  
接下來的 (Q) 行中，**對於每筆 ((r, c)) 查詢**，輸出在對應網格中，以該點為中心的最大正方形邊長。

---

如果你要，我可以下一步幫你補上：

- 💡 **解題思路（暴力 / 優化）**
    
- 💻 **C / C++ / Python 範例解**
    
- 🧠 **為什麼只需要檢查奇數邊長**
    
- ⚠️ **常見 WA / TLE 雷點**
    

直接說你要哪個 👍