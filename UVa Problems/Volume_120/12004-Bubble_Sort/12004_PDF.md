# 12004 - Bubble Sort

Check the following code which counts the number of swaps of bubble sort.

```c
int findSwaps( int n, int a[] )
{
    int count = 0, i, j, temp, b[100000];

    for( i = 0; i < n; i++ ) {
        b[i] = a[i];
    }
    for( i = 0; i < n; i++ ) {
        for( j = 0; j < n - 1; j++ ) {
            if( b[j] > b[j+1] ) {
                temp = b[j];
                b[j] = b[j+1];
                b[j+1] = temp;

                count++;
            }
        }
    }

    return count;
}
```

You have to ==find the average value of **count**== in the given code if we run `findSwaps()` infinitely many times using constant $n$ and each time some random integers (from 1 to $n$) are given in array `a[]`. You can assume that the input integers in array `a[]` are distinct.

---

## Input

- Input starts with an integer $T$ ($\le 1000$), denoting the number of test cases. 
- Each test case contains an integer $n$ ($1 \le n \le 10^5$) in a single line.

---

## Output

For each test case, print the case number and the desired result. 

- If the result is an integer, print it. 
	- Otherwise print it in `p/q` form, where $p$ and $q$ are relatively prime.

---

## Sample Input

```
2
1
2
```

---

## Sample Output

```
Case 1: 0
Case 2: 1/2
```

---

# 中文翻譯

請注意以下程式碼，它會計算泡沫排序（bubble sort）的交換次數。

```c
int findSwaps( int n, int a[] )
{
    int count = 0, i, j, temp, b[100000];

    for( i = 0; i < n; i++ ) {
        b[i] = a[i];
    }
    for( i = 0; i < n; i++ ) {
        for( j = 0; j < n - 1; j++ ) {
            if( b[j] > b[j+1] ) {
                temp = b[j];
                b[j] = b[j+1];
                b[j+1] = temp;

                count++;
            }
        }
    }

    return count;
}
```

現在固定 $n$，無限次執行 `findSwaps()`，且每次陣列 `a[]` 都給定一組由 $1$ 到 $n$ 所組成的隨機排列（且所有數字皆相異）。

==請你求出 `count` 的**平均值**==。

---

## 輸入

- 第一行為一個整數 $T$（$\le 1000$），表示測資的筆數。
	- 每筆測資包含一個整數 $n$（$1 \le n \le 10^5$）。

---

## 輸出

對於每筆測資，請輸出測資編號與所求的結果。

- 若結果為整數，直接輸出該整數；
- 否則請以 `p/q` 形式輸出，其中 $p$ 與 $q$ 互質。