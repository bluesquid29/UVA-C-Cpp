[Complement of Base 10 Integer - LeetCode](https://leetcode.com/problems/complement-of-base-10-integer/)


# Code

```cpp
int mask = 1;

while((mask & n) != n)
{
	mask = mask << 1;
	mask += 1;
}

return (mask ^ 1);
```